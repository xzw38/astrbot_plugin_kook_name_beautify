# Changelog

## 0.4.1 - 2026-07-29

- 新建分组时强制所有新文字和语音频道提供 `parent_ref`，避免 AI 生成根目录频道。
- 创建完成后重新读取 KOOK 实时结构，逐一核验新频道是否进入计划分组。
- 分组归属不正确时自动清理本次新建频道；永久替换会在删除任何旧频道前终止。

## 0.4.0 - 2026-07-28

- 新增管理员专用的完整结构永久替换模式，支持一个方案内创建、改名和批量删除。
- 永久替换严格按“创建新模板、改名复用频道、删除旧子频道、删除旧分组”顺序执行。
- 自动识别并保护当前机器人回复频道，无法识别时拒绝生成批量删除方案。
- 新增 kook_apply_replacement_plan、/kook替换确认 和 /kook_replace_confirm。
- 普通美化确认与单频道删除确认均不能执行批量替换。
- 一旦进入永久删除阶段便不可撤销；若中途失败会停止后续破坏并要求人工检查。

## 0.3.0 - 2026-07-28

- 新增自然语言单频道永久删除预览工具和独立确认执行工具。
- 新增 /kook删除确认 与 /kook_delete_confirm 管理员命令。
- 删除方案不能混合创建或改名，普通美化确认无法执行删除。
- 删除前重新校验频道 ID、名称和类型；非空分组拒绝删除。
- LLM 删除工具和删除命令均强制校验 AstrBot 管理员身份，并兼容 allowed_user_ids 额外白名单。
- 永久删除无法恢复频道内容、消息和权限，预览和结果均明确警告。

## 0.2.4 - 2026-07-28

- 管理员在原话中明确提供的数字父分组 ID 可进入创建方案，即使频道列表暂时未返回该分组。
- 显式父级标记为等待 KOOK 验证，确认执行时由 channel/create 返回最终有效性结果。
- AI 不得因显式父分组未出现在实时列表而返回空 creates 或自定义 error。

## 0.2.3 - 2026-07-28

- 识别“新建、创建、新增频道”等明确创建意图，并强制要求 AI 返回非空 creates。
- 空方案重试时明确要求至少创建一个分组或频道，避免连续返回空数组。
- 调试模式新增 AI 方案输出日志，便于定位模型未按结构规划的问题。

## 0.2.2 - 2026-07-28

- 修复 KOOK 删除分组后，子频道仍残留旧 `parent_id`，导致 AI 反复引用无效分组的问题。
- 发送给 AI 前只保留当前真实存在分组的父级引用，并强化 `parent_ref` 约束提示。

## 0.2.1 - 2026-07-28

- AI 引用已删除或失效频道 ID 时，自动刷新 KOOK 当前频道列表并重新规划一次。
- 重试提示会要求 AI 丢弃旧频道 ID，只使用最新频道清单。
- 预览中明确显示当前频道名称已作为本方案的内存撤销备份。

## 0.2.0 - 2026-07-28

- 自然语言方案新增 `creates`，支持一键创建分组、文字频道和语音频道。
- 新频道可通过临时编号归属同方案新建分组，也可归属现有分组。
- 支持语音频道人数上限与音质参数，并严格校验类型、父级、名称和总操作数。
- 应用失败时恢复已改名频道，并反序清理本次新建频道。
- 撤销会删除本方案创建的频道并恢复改名；检测到后续人工改动或分组内新增外部频道时拒绝误删。

## 0.1.3 - 2026-07-28

- 新增 `kook_apply_beautify_plan` LLM 工具，使明确确认可通过 AI 工具执行，不再完全依赖斜杠命令路由。
- 新增 `kook_rollback_beautify_plan` LLM 撤销工具。
- 执行和撤销工具同时校验 `confirm=true`、当前原始消息中的动作文字及相同方案编号。
- 新增 `/kook_beautify_confirm` 和 `/kook_beautify_rollback` 英文备用命令。

## 0.1.2 - 2026-07-28

- 增加可配置的 KOOK API、改名进度、冲突检查和回滚调试日志。
- 执行或撤销失败时保留 KOOK API 的原始错误码与错误消息。
- 调试日志不会输出 Bot Token 或 Authorization。

## 0.1.1 - 2026-07-28

- 修复 AstrBot 以插件包路径加载时无法导入 `beautify` 和 `kook_api` 的问题。

## 0.1.0 - 2026-07-28

- 首个版本。
- 支持 AstrBot LLM 工具和自然语言频道名称规划。
- 支持方案预览、管理员确认、执行冲突检查和撤销。
- 支持 KOOK API 分页、限流重试和批量失败恢复。
- 增加插件市场元数据同步脚本与自动测试。
